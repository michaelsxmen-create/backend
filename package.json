{
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "test": "jest --runInBand",
    "postinstall": "cd backend && yarn install --production"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "mongodb-memory-server": "^11.0.1",
    "supertest": "^7.1.4"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "mongodb": "^7.0.0"
  }
}
